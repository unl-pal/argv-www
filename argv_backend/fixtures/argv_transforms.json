[
    {
        "model": "website.transform",
        "fields": {
            "name": "ARG-V integer transformer",
            "enabled": false,
            "associated_backend": ["argv"]
        }
    },
    {
        "model": "website.transformparameter",
        "fields": {
            "transform": ["ARG-V integer transformer"],
            "name": "minExpr",
            "val_type": "Integer",
            "default_val": "1",
            "help_text": "Minimum number of expressions. Must be >= 1. If not specified, uses 1."
        }
    },
    {
        "model": "website.transformparameter",
        "fields": {
            "transform": ["ARG-V integer transformer"],
            "name": "minIfStmt",
            "val_type": "Integer",
            "default_val": "0",
            "help_text": "Minimum number of if statements. Must be >= 0. If not specified, uses 0."
        }
    },
    {
        "model": "website.transformparameter",
        "fields": {
            "transform": ["ARG-V integer transformer"],
            "name": "minParams",
            "val_type": "Integer",
            "default_val": "0",
            "help_text": "Minimum number of parameters. Must be >= 0. If not specified, uses 0."
        }
    },
    {
        "model": "website.transformparameter",
        "fields": {
            "transform": ["ARG-V integer transformer"],
            "name": "target",
            "val_type": "String",
            "default_val": "DEF",
            "help_text": "Two options: DEF (default behavior) or SPF (for Symbolic Path Finder)"
        }
    }
]
